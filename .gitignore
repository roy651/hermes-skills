# Secrets
.env
*.pem
*.key
*.p12
*.jks

# Python
__pycache__/
*.pyc
.pytest_cache/
.venv/

# Temporary files
*.tmp
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

navman/test_data/
navman/data/
navman/logs/
data/
!wyckoff/data/factor_tags.yaml
!wyckoff/data/secnum_map.example.json
# Personal/specific trading config (watchlist/parked/levels) — treated like holdings, kept
# out of git. Runtime-only; copy config.example.yaml → config.yaml to bootstrap.
wyckoff/config.yaml
# ...and any timestamped backup of it — same PII, and the bare
# filename pattern above does not cover them.
wyckoff/config.yaml.bak-*
wyckoff/data/holdings.bak-*
logs/
.claude/

# haaretz-puzzler runtime artifacts (cache + downloaded puzzle images)
haaretz-puzzler/cache_state.json
haaretz-puzzler/output/

# wyckoff portfolio-ticker reference doc (PII); config.yaml handled above, holdings/secnum via data/
wyckoff/references/roy-portfolio-tickers.md
# research bench price/observation caches (~1GB, regenerable, no PII but huge)
wyckoff/research/cache/

# finance-assistant encrypted bank config (age) — never in git
finance-assistant/importer/config.enc
