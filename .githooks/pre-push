#!/usr/bin/env bash
# Pre-push hook — no hostname block.
# The real PII guardrail is the pre-commit + commit-msg hooks (they reject holdings/positions/
# quantities/secnum data before a commit is ever made). The agent is allowed to push skill fixes
# directly; just run `git pull` on the Mac before editing after an agent push.
exit 0
